function sumOfIntegersInString(s) {

    let number = s.match(/\d+/g);
    
    if (numbers) {

        let count = number.reduce(function(str, num) {

            return str + parseInt(num , );

        }, 
        0);

        return count; 

    } 
    else {

        return 0; 

    }
}

let string = "avazbek2002awo3594qaxxorov";

let res = sumOfIntegersInString(string)

console.log(res);